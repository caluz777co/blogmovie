import { NumericLiteral } from "typescript";

export class Pelicula {
  id: number;
  title: string;
  url_image: string;
  id_categoria: number;
  like: number;
  description: string;
  year: string;
  director: string;
  status: number;
}
